LangChain的流式输出
```python

agent = create_agent(  
    model=model,  
    tools=[get_weather],  
    checkpointer=InMemorySaver()  
)  
config: RunnableConfig = {'configurable': {'thread_id': '001'}}  
# 返回一个流模式迭代器  
input_content = {'messages': [HumanMessage('贵阳天气怎么样啊,适合外出吗?')]}  
stream_iterator = agent.stream(input_content, config=config, stream_mode=['messages'],version='v1或v2,v3')
for chunk in stream_iterator:
	1.当version=v1,默认v1
		stream_mode=['messages']
			return: ("messages", (AIMessageChunk, meta))
		stream_mode='messages'
			return: (AIMessageChunk, metadata)
		stream_mode=['messages','custom']
			return: ("messages", ...) 和 ("custom", ...) 交替 yield
	2.当version=v2 stream_mode=任意
		return: {'type': mode,'ns':(), 'data': payload}
	3.当version=v3,不适用.
		agent.stream() 不支持 v3, version 只接受 'v1' | 'v2'
		v3 只能用来调用 agent.stream_events(version='v3')，返回的是 GraphRunStream 对象
	
	注意: v1和v2的取值方式不同,需要自己进行条件判断
	
```

> [!caution]  警告
> 为了统一API，建议在使用流式输出的时候都统一使用 version='v2'

```python
这是v2的取值方式:
	for chunk in stream_iterator:  
	    print(f"stream_mode: {chunk['type']}")  
	    print(f"content: {chunk['data']}")  
	    if chunk['type'] == 'updates':  
	        for step, data in chunk['data'].items():  
	            print(f'step: {step}')  
	            print(f"data: {data['messages'][-1].content_blocks}")  
	      
	    if chunk['type'] == 'messages':  
	        token, metadata = chunk['data']  
	        print(f'token: {token.content}')  
	    print(f'metadata: {metadata}')  
	      
	    if chunk['type'] == 'custom':  
	        print(f'custom: {chunk['data']}')
stream_mode not None时:v1的取值方式:
	for mode, data in stream_iterator:  
	        token, message = data  
	        if not isinstance(token, AIMessageChunk):  
	            continue  
	        reasoning = [b for b in token.content_blocks if b['type'] == 'reasoning']  
	        text = [b for b in token.content_blocks if b['type'] == 'text']  
	  
	        if reasoning:  
	            print(f"{reasoning[0]['reasoning']}", end='')  
	  
	        if text:  
	            print(f"{text[0]['text']}", end='')
	v1取值时从token.content_blocks中获取
```

##  ==**stream_it = agent.stream(input=input_content,version='',stream_mode='')**==
## 这里的参数==**version**==(默认就是v1),和==**stream_mode**==很关键,参数不同返回的数据结构就不同非常容易出错, 所以后续的版本
## 官方也强烈建议通通使用==**version='v2'**== 这个版本的数据响应格式来处理, ==**version='v2'**== 不管stream_mode怎么变数据结构都是一样的

```python
stream_it = agent.stream(input=input_content) # 默认version=v1, stream_mode=None
for state in stream_it:
	state的数据格式如下,并且v1模式下并支持流式输出,而是处理完成后的消息响应
"""
{
    'model': {
        'messages': [
            AIMessage(
                content='LangChain是一个开源框架，用于构建基于大型语言模型的应用程序，通过链式调用、工具集成和内存管理等模块化设计，简化AI工作流的开发与部署。',
                additional_kwargs={
                    'reasoning_content': '我们要求写一句话介绍LangChain。LangChain是一个用于构建LLM应用的框架，主要特点是链式调用、工具集成、记忆等。一句话需要简洁准确。'
                },
                response_metadata={
                    'finish_reason': 'stop',
                    'model_name': 'deepseek-v4-flash',
                    'system_fingerprint': 'fp_8b330d02d0_prod0820_fp8_kvcache_20260402',
                    'model_provider': 'deepseek'
                },
                id='lc_run--019ed525-4073-78e0-bce4-330f9d4288f3',
                tool_calls=[],
                invalid_tool_calls=[],
                usage_metadata={
                    'input_tokens': 10,
                    'output_tokens': 72,
                    'total_tokens': 82,
                    'input_token_details': {
                        'cache_read': 0
                    },
                    'output_token_details': {
                        'reasoning': 35
                    }
                }
            )
        ]
    }
}
"""

```

```python 

stream_it = agent.stream(input=input_content) # 默认version=v1, stream_mode=None
for chunk in stream_iterator:
其他的情况返回数据结构的总结:

	1.当version=v1,默认v1
		stream_mode=None
			return dict:{'model':{'messages':[AIMessage()]}}
		stream_mode=['messages']
			return: ("messages", (AIMessageChunk, meta))
		stream_mode='messages'
			return: (AIMessageChunk, metadata)
		stream_mode=['messages','custom']
			return: ("messages", ...) 和 ("custom", ...) 交替 yield
	2.当version=v2 stream_mode=任意
		return: {'type': mode,'ns':(), 'data': payload}
	3.当version=v3,不适用.
		agent.stream() 不支持 v3, version 只接受 'v1' | 'v2'
		v3 只能用来调用 agent.stream_events(version='v3')，返回的是 GraphRunStream 对象
```