软件安装:
1.nginx:
docker run -d -p 7080:7080 --name nginx -v /mydata/nginx/html:/usr/share/nginx/html -v /mydata/nginx/conf/nginx.conf:/etc/nginx/nginx.conf -v /mydata/nginx/logs:/var/log/nginx nginx

```java
docker run -it --rm --name nginx --network=host 
```
-v /data/nginx/conf/nginx.conf:/etc/nginx/nginx.conf -d e791337790a6 bash
liunx安装: yum -y install policycoreutils openssh-server openssh-clients postfix zlib zlib-devel pcre-devel
 ./configure --prefix=/usr/local/nginx
make && make install

2.mysql:
	docker run --name mysql --restart=always -p 3307:3306 -e MYSQL_ROOT_PASSWORD=123456 -d mysql:5.7

```dockerfile
docker run --name mysql5.7 --restart=always -p 3306:3306 -e MYSQL_ROOT_PASSWORD=qingfeng \
-d mysql:5.7.27 \
-v /tmp/data:/var/lib/mysql-files/
--character-set-server=utf8mb4 \
--collation-server=utf8mb4_unicode_ci \
--max_allowed_packet=5120M
```

3.rabbitmq (使用--net host不管用,待解决)
	 docker run -it --rm --name rabbitmq -e RABBITMQ_DEFAULT_USER=qdy \
	 -e RABBITMQ_DEFAULT_PASS=Ycy666888 -p 15672:15672 -p 5672:5672 -d rabbitmq:management
	 
	 docker run -d -p 15672:15672 -p 5672:5672 --hostname my-rabbit --name rabbitmq 
	 -e RABBITMQ_DEFAULT_USER=qdy -e RABBITMQ_DEFAULT_PASS=Ycy666888 rabbitmq:3-management

4.window10安装mysql服务
mysqld --initialize --console --explicit_defaults_for_timestamp server binpath= "D:\DevSoftInstall\mysql-5.7.29-winx64\bin\mysqld MySQL"
mysqld install

5.docker安装minio
	docker run -p 9000:9000 --name minio 
-d --restart=always -e "MINIO_ACCESS_KEY=admin"  
-e "MINIO_SECRET_KEY=admin123456" \
-v /home/data:/data -v /home/config:/root/.minio minio/minio server /data

docker run -p 9000:9000 --name minio -d --restart=always -e 'MINIO_ACCESS_KEY=qingduo' -e 'MINIO_SECRET_KEY=qingduo2019' -v $HOME/minio/data:/data -v $HOME/minio/config:/root/.minio 37990700d181 server /data

初始化服务器脚本:
	curl http://pigx.vip/os7init.sh | sh -s aligzulmc
安装docker
	curl http://pigx.vip/docker_install.sh | sh
	
webdev/root /data/appdata/webdev/nginx/conf/qingduo-back/nginx.conf

sudo docker run --net=host -p 80:80 -p 9092:9092 --add-host qingduo-gateway:172.27.16.6 
-v /etc/localtime:/etc/localtime 
-v /data/appdata/webdev/nginx/logs/qingduo-back:/var/log/nginx:rw 
-v /data/appdata/webdev/nginx/conf/qingduo-back/nginx.conf:/etc/nginx/nginx.conf 
-v /data/appdata/webdev/nginx/html/qingduo-back:/usr/share/nginx/html:rw 
-v ~/markdown:/usr/share/nginx/markdown:rw -d nginx:latest

6. 安装elasticsearch,kibana,IK分词器
	docker run --name elasticsearch -p 9200:9200 -p 9300:9300 -e "discovery.type=single-node" elasticsearch:6.8.4
	docker run --name es682 --net esnet -p 9200:9200 -p 9300:9300 -e "discovery.type=single-node" -d elasticsearch:6.8.4
	docker run --name kibana682 --net esnet --link 6098771bd491:elasticsearch -p 5601:5601 -d kibana:6.8.4
	docker run --name kibana682 --net esnet --elasticsearch.url=http://es682:9200 -p 5601:5601 -d kibana:6.8.4
	docker run --link YOUR_ELASTICSEARCH_CONTAINER_NAME_OR_ID:elasticsearch -p 5601:5601 {docker-repo}:{version}
	docker run --name kibana681 --net esnet --link e3809f67da81:elasticsearch -p 5601:5601 -d kibana:6.8.4

7.kibana安装
docker run --name kibana681 --net esnet --link 6098771bd491:elasticsearch -p 5601:5601 -d kibana:6.8.4
插件:


	cd /usr/share/elasticsearch/plugins/
	elasticsearch-plugin install https://gitee.com/lmcloveyby/elasticsearch-analysis-ik/repository/archive/v.zip
	exit
	docker restart elasticsearch 

8. ES安装: ES_JAVA_OPTS:配置ES内存大小
,修改ES文件相关挂载文件权限 
docker run --name elasticsearch -p 9200:9200 -p 9300:9300 -e "discovery.type=single-node" -e ES_JAVA_OPTS="-Xms64m -Xmx512m" -v 	/mydata/elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml -v /mydata/elasticsearch/data:/usr/share/elasticsearch/data -v  /mydata/elasticsearch/plugins:/usr/share/elasticsearch/plugins -v /mydata/elasticsearch/logs:/usr/share/elasticsearch/logs -d elasticsearch:7.9.3
9. kibana安装:
--restart=always
docker run --name kibana --rm --link 8c7f91c03e67:elasticsearch -p 5601:5601 -d kibana:6.8.4
docker run --name kibana --rm -e ELASTICSEARCH_HOSTS=http://121.37.212.183:9200 -p 5601:5601 -d kibana:7.9.3
docker run --name kibana --rm -e ELASTICSEARCH_HOSTS=http://192.168.113.232:9200 -p 5601:5601 -d kibana:7.9.3
docker run --name kibana --rm -e ELASTICSEARCH_HOSTS=elasticsearch:9200 -p 5601:5601 -d kibana:7.9.3

10.nginx安装:
	docker run -d -p 80:80 --read-only -v $(pwd)/nginx-cache:/var/cache/nginx -v $(pwd)/nginx-pid:/var/run nginx
	docker run -d -p 80:80 --name myNginx -v /host/path/nginx.conf:/etc/nginx/nginx.conf:ro nginx
	docker run -d -p 80:80 --rm --name nginx -v /mydata/nginx/nginx.conf:/etc/nginx/nginx.conf -v /mydata/nginx/logs:/etc/nginx/logs nginx
	docker run -d -p 80:80 --rm --name nginx  nginx
	
	docker run --name nginx --rm -p 80:80 -v /mydata/nginx/conf/nginx.conf:/etc/nginx/nginx.conf -v /mydata/nginx/logs/:/etc/nginx/logs/ -d daee903b4e43
	docker run --name nginx --restart=always -p 80:80 -v /mydata/nginx/conf/nginx.conf:/etc/nginx/nginx.conf -d nginx:1.16.1
	
	docker run -p 80:80 --name nginx --restart=always -v /mydata/nginx/html:/usr/share/nginx/html -v /mydata/nginx/conf/nginx.conf:/etc/nginx/nginx.conf -d nginx:1.16.1
	
	docker container cp nginx:/etc/nginx/*  /mydata/nginx/conf/ 

tomcat安装:
	docker run -p 8081:8080 --rm --name tomcat1 -v /mydata/tomcat/webapps:usr/local/tomcat/webapps -d tomcat:8.0

MySql安装:
	docker run -it --link some-mysql:mysql --rm daocloud.io/mysql sh -c 'exec mysql -h"$MYSQL_PORT_3306_TCP_ADDR" -P"$MYSQL_PORT_3306_TCP_PORT" -uroot -p"$MYSQL_ENV_MYSQL_ROOT_PASSWORD"'

logstash安装:
	docker run --rm -it -v /mydata/logstash/logstash.yml:/usr/share/logstash/config/logstash.yml -d fc2df485e5c5
	docker run -d --rm --log-driver json-file --log-opt max-size=100m --log-opt max-file=2 -p 5044:5044 --name logstash  \
	-v /mydata/logstash/logstash.yml:/usr/share/logstash/config/logstash.yml  \
	-v /mydata/logstash/conf.d:/usr/share/logstash/conf.d logstash:7.9.3



tomcat部署war包方式安装jenkins
1.首次登陆忘记密码
修改 /root/.jenkins/config.xml 去除配置
<useSecurity>true</useSecurity> 
<authorizationStrategy class="hudson.security.FullControlOnceLoggedInAuthorizationStrategy"> 
 <denyAnonymousReadAccess>true</denyAnonymousReadAccess> 
     </authorizationStrategy>
登陆jenkis创建用户
重启Jenkins服务（如果是war包方式的部署，即重启tomcat）；
进入首页>“系统管理”>“Configure Global Security”；
勾选“启用安全”；
点选“Jenkins专有用户数据库”，并点击“保存”；
重新点击首页>“系统管理”,发现此时出现“管理用户”；
点击进入展示“用户列表”；
点击右侧进入修改密码页面，修改后即可重新登录